<%= turbo_stream.update "customers-table-body" do %>
  <%= render partial: "customer_row", collection: @customers, as: :customer %>
<% end %>

<%= turbo_stream.update "flash-container" do %>
  <%= render "shared/flash_messages" %>
<% end %>

<%= turbo_stream.update "customer_form" do %>
  <div class="text-center text-muted p-4">
    <i class="bi bi-building fs-1 d-block mb-2"></i>
    Select an action to load the form.
  </div>
<% end %>